import { html } from "cv";

export default function App() {
    return html`<>
        <h2>Hello world.</h2>
        <p>This is an App component virtualized from the DOM.</p>
        This is a text node, not an element.
    </>`;
}
